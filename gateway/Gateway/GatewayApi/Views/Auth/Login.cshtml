@{
    ViewData["Title"] = "Вход";
}

<div class="row justify-content-center mt-4">
    <div class="col-12 col-md-8 col-lg-5">
        <div class="app-card">
            <div class="app-card-header">
                <h2 class="app-card-title">Вход в систему</h2>
            </div>

            <div class="p-4">
                @if (ViewBag.Error != null)
                {
                    <div class="alert alert-danger mb-3">
                        @ViewBag.Error
                    </div>
                }

                <form method="post" action="@(Context.Request.PathBase + "/auth/login")">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="returnUrl" value="@ViewBag.ReturnUrl" />

                    <div class="mb-3">
                        <label for="login" class="form-label">Логин</label>
                        <input id="login" name="login" type="text" class="form-control" autocomplete="username" />
                    </div>

                    <div class="mb-3">
                        <label for="password" class="form-label">Пароль</label>
                        <input id="password" name="password" type="password" class="form-control" autocomplete="current-password" />
                    </div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-app-primary">Войти</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>